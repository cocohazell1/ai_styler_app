[theme]
primaryColor="#F63366"        # 기본 강조 색상 (핑크 계열)
backgroundColor="#FFFFFF"     # 배경색 (흰색)
secondaryBackgroundColor="#F0F2F6" # 두 번째 배경색 (약간 회색)
textColor="#262730"           # 기본 텍스트 색상
font="sans serif"             # 기본 폰트

[browser]
gatherUsageStats = false      # 사용 통계 수집 비활성화

[server]
# headless = true             # 브라우저 자동 실행 안 함 (선택적)
# runOnSave = true            # 파일 저장 시 자동 새로고침 (선택적)